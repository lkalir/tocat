[profile.ci]
fail-fast = false
failure-output = "immediate-final"
status-level = "fail"
final-status-level = "flaky"
# Tests may timeout, cap their runtime
slow-timeout = { period = "30s", terminate-after = 4 }
retries = 0

[profile.ci.junit]
path = "junit.xml"
